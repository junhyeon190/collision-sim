# 충돌·안전장치 수업 시뮬레이터 — 개발 명세서 (확정판)

성취기준 [10통과1-03-04] · 통합과학1 · 4차시 구성

---

## 0. 이 문서의 위치

이 프로젝트는 **웹 앱 하나**이며, 내부에 두 개의 모듈이 있다.

| 모듈 | 사용 차시 | 경로 |
|---|---|---|
| 오개념 실험실 | 1차시 | `/lab` |
| 설계 스튜디오 | 3차시 | `/studio` |
| 교사 화면 | 1·3차시 | `/teacher` |

2차시(완충재 개수별 실측)와 4차시(개별 서술평가)는 **시뮬레이터를 사용하지 않는다.**
2차시는 스마트폰 가속도 센서를 이용한 실물 실험이며, 그 측정값은 3차시 설계 스튜디오에 입력되어 재사용된다.

**설계 원칙 세 가지**

1. 시뮬레이터는 실물 실험이 보여줄 수 없는 것만 보여준다. (충격량 = F–t 그래프의 면적)
2. 학생이 예측을 저장하기 전에는 결과를 볼 수 없다.
3. 정답을 먼저 말하지 않는다. 학생의 예측과 관찰 결과의 차이를 드러내는 것이 목적이다.

---

## 1. 기술 스택 (변경 금지)

- React 18 + TypeScript + Vite
- Tailwind CSS
- Zustand (상태 관리)
- Recharts (그래프)
- 애니메이션: `requestAnimationFrame` + SVG (외부 애니메이션 라이브러리 금지)
- 저장: **브라우저 localStorage 전용. 서버·백엔드·Firebase 일절 사용하지 않는다.**
- 라우팅: react-router-dom

### 절대 규칙

- **네트워크 요청을 만들지 않는다.** fetch, axios, 외부 API 호출 금지. 학교망에서 차단되면 수업이 멈춘다.
- **AI API를 호출하지 않는다.** 3차시 AI 활용은 "프롬프트 문자열을 만들어 클립보드에 복사"까지만 담당한다.
- **HTML5 Drag and Drop API 사용 금지.** 갤럭시탭에서 동작하지 않는다. 상세는 §6.

---

## 2. 대상 기기와 레이아웃

| 기기 | 해상도 기준 | 레이아웃 |
|---|---|---|
| 노트북 | 1366×768 이상 | 3분할 |
| 갤럭시탭 가로 | 1280×800 | 3분할 (동일) |
| 좁은 화면 | 1024px 미만 | 시뮬레이션 상단 고정 + 하단 탭 전환 |

- 세로 방향 감지 시 "가로로 돌려 주세요" 안내 화면 표시
- 모든 터치 대상 최소 44×44 px
- 본문 16px 이상, 변인 카드 글자 16px 이상
- 색상만으로 정보를 구분하지 않는다 (색 + 아이콘 + 글자 병기)

### 공통 화면 골격

```
┌──────────────────────────────────────────────┐
│ ①─②─③─④─⑤   핵심 질문        학번 K7  ?  │
├───────────┬────────────────────┬─────────────┤
│  나의 예상  │    시뮬레이션        │   관찰 결과   │
│   26%     │        44%          │     30%     │
├───────────┴────────────────────┴─────────────┤
│  처음 생각 → 관찰한 증거 → 수정한 설명           │
└──────────────────────────────────────────────┘
```

---

## 3. 물리 엔진 명세

물리 계산은 `src/engine/` 안의 **순수 함수**로만 작성한다. React에 의존하지 않는다.
애니메이션과 그래프는 **같은 계산 결과 하나를 공유한다.** 각자 따로 계산하면 안 된다.

### 3.1 수레 (1차시 화면 1)

```
질량 m = 1.0 kg
0 ~ 1.0초 : 손이 미는 힘 F = 2.0 N (오른쪽)
  → a = 2.0 m/s²,  1.0초 시점 속도 v = 2.0 m/s
1.0초 이후 : 손을 뗌
  → 수평 알짜힘 = -μmg  (μ는 마찰 슬라이더 값, 기본값 0)
```

- μ = 0 (기본값): 손을 뗀 뒤 v–t 그래프가 완전한 수평선
- μ > 0: 속도가 직선으로 감소하다 정지
- 마찰 슬라이더는 **관찰이 끝난 뒤에만 열린다.** (§5 화면1 참조)

출력: `{ t[], F[], v[], a[], x[] }` 시계열 배열, 20 ms 간격

### 3.2 충돌 (1차시 화면 0·2, 3차시 설계 스튜디오 공용)

```
v = √(2gh)                      충돌 직전 속도
s = s0 + d · c · n^0.85         정지 거리
Δt = 2s / v                     충돌 시간
F_avg = m·v / Δt                평균힘
F_peak = (π/2) · F_avg ≈ 1.57 · F_avg
F(t) = F_peak · sin(π t / Δt)   반정현파 힘 파형
J = m · v                       충격량 (= 운동량 변화량)
```

| 기호 | 의미 | 값 |
|---|---|---|
| g | 중력가속도 | 9.8 m/s² |
| m | 낙하체 질량 | 0.2 kg (스마트폰 모형) |
| h | 낙하 높이 | 1.0 m (1차시 고정) |
| s0 | 바닥 자체 변형 | 맨바닥 0.5 mm |
| d | 완충재 한 장 두께 | 5 mm |
| c | 압축 한계율 | 0.7 |
| n | 완충재 장수 | 0 ~ 5 |

**지수 0.85가 중요하다.** 완충재를 2배로 해도 충돌 시간이 정확히 2배가 되지 않는다.
"완충재 수와 충격력은 정확한 반비례"라는 오개념을 엔진이 스스로 반박하게 만드는 장치다. 절대 1.0으로 바꾸지 말 것.

**계수 1.57도 중요하다.** 평균힘과 최대힘이 항상 다르다는 것을 학생이 표에서 보게 된다.

#### bottoming out (바닥까지 눌림)

정지 거리 계산값 `s`가 실제 압축 가능 두께 `n · d · c`를 넘으면, 완충재가 다 눌린 것이므로
초과분은 흡수되지 않고 `s = s0 + n·d·c` 로 잘린다. 이때 힘이 급등한다.
→ **완충재 1장에서 이 현상이 걸리도록 상수를 조정한다.**

#### 충격량 표기 주의

화면에는 반드시 **"충격량(알짜)"** 으로 표기한다.
엄밀히는 바닥이 준 충격량 = `m·Δv + m·g·Δt` 이지만, 통합과학1에서는 다루지 않는다.
표기를 이렇게 해 두면 심화 질문이 나와도 설명이 어긋나지 않는다.

### 3.3 실측값 역산 (3차시 전용)

학생이 2차시에 측정한 **최대 가속도(g 단위)** 로부터 되짚는다.

```
a_peak(m/s²) = 입력값(g) × 9.8
a_avg = a_peak / 1.57
Δt = v / a_avg = 1.57 · v / a_peak
F_avg = m · a_avg
J = m · v
```

---

## 4. 오개념 태그 체계

1차시에서 사용하는 태그는 **7개**로 확정한다. 학생 화면에는 태그 코드를 절대 노출하지 않는다. 교사 화면 전용.

| 태그 | 오개념 | 등장 | 반박 증거 |
|---|---|---|---|
| IMP-1 | 운동 방향으로 힘이 계속 필요하다 | 화면1 | 손 뗀 후 F=0인데 v 유지 |
| IMP-2 | 처음 받은 힘이 물체에 저장·소모된다 | 화면1 | F 그래프가 즉시 0으로 |
| IMP-3 | 힘이 없으면 물체는 자연히 정지한다 | 화면1 | μ=0에서 멈추지 않음 |
| COL-1 | 완충재가 충격량 자체를 줄인다 | 화면0·2 | 두 곡선 면적 동일 |
| COL-2 | 충격량과 충격력을 같은 것으로 본다 | 화면2 | 높이는 다른데 면적 같음 |
| GR-1 | 그래프 높이 = 면적 | 화면2 | 낮고 넓은 곡선의 면적 |
| VAR-1 | 변인을 통제할 필요가 없다 | 화면3 | 되묻기 피드백 |
| SCI-1 | (오개념 아님) 과학적으로 타당한 응답 | 전 화면 | — |

BOU-1(튕김 여부와 운동량 변화)은 1차시에서 다루지 않는다. 3차시 심화로 미룬다.

---

## 5. 1차시 화면별 스토리보드

### 공통 사항

- 낙하체는 **전 화면에서 스마트폰 모형(사각 블록)으로 통일**한다. 공을 쓰지 않는다. 사각 블록은 튀지 않는 것이 자연스러워, "충돌 후 정지" 조건이 직관과 충돌하지 않는다.
- **확신도는 화면0(최초)과 화면5(수정 후) 딱 2회만** 묻는다.
- **최초 응답은 저장 즉시 잠긴다.** 저장 버튼 클릭 시 "저장하면 수정할 수 없습니다" 확인창을 띄운다. 수정은 화면5에서만 가능.
- **자동 피드백 문구는 교사가 열어야 보인다.** 기본값 잠금. 학생은 그래프·수치까지만 본다.
- **수치 표시는 화면2까지 숨김**(그래프 모양과 면적만), 화면3부터 표시. 교사가 토글로 뒤집을 수 있다.

---

### 화면 0 — 시작 안내

**제목:** 충격을 줄인다는 것은 무엇을 줄이는 것일까?

**장면:** 같은 스마트폰 모형이 같은 높이에서 낙하. 왼쪽은 시멘트 바닥, 오른쪽은 방석. 충돌 순간 느린 재생 반복. 그래프와 수치 없음.

**질문:** 두 물체는 충돌 직전까지 같은 운동을 했다. 그런데 결과가 달라 보이는 까닭은?

**입력 1 (서술):** 방석이 충격을 줄이는 까닭은 ________ 때문이다.

**입력 2 (선택):** 방석이 주로 줄이는 것은?
1. 떨어지는 속도를 늦춰준다
2. 충돌하는 시간을 길게 늘려준다 → **SCI-1**
3. 물체가 받는 중력을 줄여준다
4. 충돌할 때 튕겨나가게 한다
5. 충격량 자체를 없애준다 → **COL-1**

(구현 단계에서 실제 선택지 문구가 위와 같이 확정되었다. 태그가 없는 1·3·4번은 확립된 7개 태그 체계 밖의 오답이며, 4번은 BOU-1(튕김)과 관련되지만 BOU-1은 1차시에서 다루지 않으므로 집계하지 않는다.)

**입력 3 (확신도):** 1~5

**버튼:** `내 생각 저장하고 탐구 시작하기`

정답·오답 표시 없음.

---

### 화면 1 — 손을 뗀 수레

**제목:** 손을 뗀 뒤에도 수레를 앞으로 미는 힘이 남아 있을까?

**장면:** 수평 레일 위의 수레. 0~1초 손이 밀고, 1초에 손을 뗌. **마찰 없음(에어트랙)으로 명시.** 아직 재생하지 않는다.

**예측 1:** 손을 뗀 뒤 수레에 작용하는 수평 방향 힘은?
1. 오른쪽으로 미는 힘이 계속 작용한다 → **IMP-1**
2. 오른쪽 힘이 점차 줄어든다 → **IMP-2**
3. 왼쪽 마찰력만 작용한다 → **IMP-3**
4. 수평 방향 알짜힘은 0이다 → **SCI-1**

**예측 2:** 손을 뗀 뒤 수레의 운동은?
1. 즉시 멈춘다 → **IMP-3**
2. 점점 느려져 멈춘다 → **IMP-2**
3. 거의 일정한 속도로 계속 움직인다 → **SCI-1**
4. 계속 빨라진다 → **IMP-1**

**이유 선택:**
1. 물체가 움직이려면 운동 방향의 힘이 필요하다 → **IMP-1**
2. 처음 받은 힘이 수레 안에 남아 있다 → **IMP-2**
3. 힘이 없어지면 물체는 원래 정지 상태로 돌아간다 → **IMP-3**
4. 알짜힘이 0이면 속도가 변하지 않는다 → **SCI-1**
5. 기타 (직접 입력)

**저장 후 재생 버튼 활성화.**

**관찰 화면:** 수레 애니메이션 + 힘 화살표 / F–t 그래프 / v–t 그래프 / 손 뗀 시점 세로선

**해석 질문:** 손을 뗀 뒤 힘 그래프는 0이 되었다. 속도 그래프는?
① 즉시 0 ② 천천히 0으로 감소 ③ 거의 일정하게 유지 ④ 계속 증가 — **정답 ③**

**마찰 슬라이더 개방 (관찰 후에만):**
> 실제 교실 바닥이라면 어떨까? 마찰을 조금씩 늘려 보세요.

μ를 0에서 0.3까지 조절. IMP-3(힘이 없으면 정지) 학생이 "멈추는 이유는 마찰"임을 스스로 만들어 보는 구간. **이 슬라이더는 필수 기능이다.**

**자동 피드백 (교사가 열면 표시):**
- IMP-1·IMP-2: "수레가 움직이는 동안 오른쪽 힘이 계속 필요하다고 예상했습니다. 그러나 손을 뗀 뒤 힘은 0이 되었는데도 속도는 유지되었습니다. '운동 중'과 '힘이 작용함'은 같은 뜻일까요?"
- IMP-3: "힘을 없애면 수레가 멈출 것이라고 예상했습니다. 시뮬레이션에서는 알짜힘이 0일 때 속도가 유지되었습니다. 실제 바닥에서 물체를 멈추게 하는 힘은 무엇일까요?"
- SCI-1: "알짜힘이 0인 구간에서 속도가 유지되는 것을 확인했습니다. 다음 충돌 상황에서도 힘과 운동 변화를 구분해 보세요."

---

### 화면 2 — 면적 불변 데모 (핵심 화면)

**제목:** 완충재는 충격량을 줄일까, 충돌하는 힘을 줄일까?

이 화면은 **실험이 아니라 데모**다. 슬라이더 없음, 반복 없음, 노이즈 없음.
학생이 조작할 수 있는 것은 재생/일시정지/느린 재생뿐이다.

**비교 대상:** 맨바닥 vs 완충재 3장 (2차시 실물 실험과 같은 재료)

**상단 고정 조건 표시:**
```
물체 질량 : 같음      낙하 높이 : 같음
충돌 직전 속도 : 같음   충돌 후 : 두 물체 모두 정지
튕김 : 없음           다른 조건 : 바닥만 다름
```

**예측표 (4행):** 각 행마다 `맨바닥이 큼 / 완충재가 큼 / 같음 / 모르겠음`

| 물리량 | 정답 |
|---|---|
| 운동량 변화량 | 같음 |
| 충격량 | 같음 |
| 충돌 시간 | 완충재가 큼 |
| 평균힘 | 맨바닥이 큼 |

- 운동량 변화량 또는 충격량에 "맨바닥이 큼" → **COL-1**
- 충돌 시간과 평균힘의 방향을 반대로 → **COL-2**

**이유 선택:**
1. 완충재가 충격을 흡수하므로 충격량이 작다 → **COL-1**
2. 같은 물체가 같은 속도로 충돌해 정지하므로 운동량 변화량은 같다 → **SCI-1**
3. 완충재는 물체의 질량을 줄인다
4. 부드러운 바닥에서는 중력이 작게 작용한다
5. 아직 모르겠다

**관찰 화면:** 두 충돌을 동시 재생 + F–t 그래프 두 곡선 겹쳐 그리기

- 맨바닥: 높고 좁은 곡선
- 완충재: 낮고 넓은 곡선
- **두 곡선 아래를 같은 색 반투명으로 칠하고, 면적 수치를 곡선 옆에 항상 표시**
- 애니메이션: 두 면적이 서로 겹쳐지며 같음을 보여주는 전환 효과 (필수)

**추가 시각화 — 물그릇 비유 토글:**
같은 양의 물을 넓고 낮은 그릇 / 좁고 높은 그릇에 담는 그림. GR-1 대응.

**빈칸 채우기:**
> 완충재는 충돌 시간을 ____하여 평균힘을 ____하지만, 같은 운동량 변화가 일어나므로 충격량은 ____.

정답: 증가 / 감소 / 거의 같다

---

### 화면 3 — 변인 카드 분류

**제목:** 완충재 개수가 충돌 결과에 미치는 영향을 어떻게 알아볼까?

**탐구 문제:** 완충재 개수가 늘어나면 스마트폰 모형의 가속도 피크값은 어떻게 변하는가?
> 다음 시간에 여러분이 직접 스마트폰으로 측정할 실험입니다. 오늘은 그 설계를 합니다.

**카드 9장** (12장에서 축소. 충격량·반복 측정 횟수는 3칸에 안 들어가므로 제외)

| 조작 변인 | 종속 변인 | 통제 변인 |
|---|---|---|
| 완충재 개수 | 가속도 피크값 | 물체 질량 |
| | 충돌 시간 | 낙하 높이 |
| | 평균힘 | 완충재 재질 |
| | | 완충재 한 장의 두께 |
| | | 충돌 후 튕김 여부 |

**오답 처리 — 정답을 알려주지 않고 되묻는다.**

낙하 높이를 조작 변인에 넣으면:
> 완충재 개수와 낙하 높이를 동시에 바꾸면, 결과가 달라진 원인을 어느 쪽 때문이라고 판단할 수 있을까요?

통제 변인 칸이 비어 있으면 다음 단계로 넘어갈 수 없다. (A수준 관문)

**기록 항목:** 첫 시도 분류 / 수정 횟수 / 최종 분류 / 분류 이유(서술)

---

### 화면 5 — 생각 비교·수정

**제목:** 처음 생각과 지금의 설명은 어떻게 달라졌을까?

**왼쪽:** 학생의 최초 응답(화면0·1·2) 자동 표시
**오른쪽:** 관찰 결과 요약 — 운동량 변화량 같음 / 충격량 같음 / 충돌 시간 증가 / 평균힘 감소

**활동:**
1. 화면0 선택형 문항 **재응답** (최초 응답과 나란히 표시)
2. 최종 확신도 1~5
3. 서술 1문장: 처음에는 ____라고 생각했지만, 관찰 결과를 보고 ____라고 생각을 수정했다.

---

### 출구 문항 (형성평가 기록용, 4문항)

1. **(선택)** 마찰을 무시할 수 있는 수평면에서 물체가 오른쪽으로 일정한 속도로 움직인다. 수평 방향 알짜힘은? ① 오른쪽 ② 왼쪽 ③ 0 ④ 속도에 비례 — **정답 ③**
2. **(선택)** 같은 물체를 같은 속도로 맨바닥과 완충재에 충돌시켜 모두 정지시켰다. 가장 적절한 설명은?
   ① 완충재에서 운동량 변화량과 충격량이 모두 작다
   ② 충격량은 비슷하지만 완충재에서 충돌 시간이 길어 평균힘이 작다 — **정답**
   ③ 완충재에서는 중력이 작게 작용한다
   ④ 맨바닥에서만 충격량이 발생한다
3. **(서술)** 완충재 개수가 가속도 피크에 미치는 영향을 알아볼 때 통제해야 할 변인 두 가지를 쓰시오.
4. **(서술)** 완충재가 충격을 줄이는 원리를 '충돌 시간'과 '평균힘'을 사용하여 설명하시오.

**완료 후 결과 코드 발급 화면으로 이동.**

---

## 6. 변인 카드 드래그 구현 (실패 위험 최상)

**HTML5 Drag and Drop API(`draggable`, `ondragstart`, `ondrop`)를 절대 사용하지 않는다.**
모바일·태블릿 브라우저가 지원하지 않아 갤럭시탭에서 카드가 움직이지 않는다.

**구현 방식**

1. **Pointer Events 기반 커스텀 드래그**
   - `onPointerDown` → `setPointerCapture` → `onPointerMove`로 좌표 갱신 → `onPointerUp`으로 드롭 판정
   - 드래그 중 카드는 `position: fixed` + `transform: translate()`
   - 상자 위에 올라오면 `document.elementFromPoint()`로 대상 판정, 상자 테두리 강조
   - 카드와 상자 모두 `touch-action: none` (스크롤과 충돌 방지)

2. **탭-투-탭 대체 조작 (필수, 선택 사항 아님)**
   - 카드를 탭 → 선택 상태(테두리 강조) → 상자를 탭 → 이동 완료
   - 드래그가 실패하는 기기에서도 반드시 활동이 진행되어야 한다

3. 상자 안의 카드를 다시 탭하면 원래 자리로 되돌아온다

---

## 7. 결과 코드 (서버 없는 교사 대시보드)

### 인코딩

학생 응답을 비트로 압축하여 Base32(Crockford) 문자열로 만든다. 총 86비트 → **18자**.

| 항목 | 비트 |
|---|---|
| 학번 | 6 |
| 화면0 선택 + 확신도 | 6 |
| 화면1 예측1·2·이유 | 7 |
| 화면2 예측표 4행 + 이유 | 11 |
| 화면3 변인 9장 첫시도/최종 + 수정 횟수 | 40 |
| 화면5 재응답 + 최종 확신도 | 6 |
| 출구 1·2 | 4 |
| 체크섬 | 6 |

표시 형식: `K7QM-3XB9-PW2F-HR5T-8J` (4자마다 하이픈, 대문자)

- 학생은 이 코드를 **학습지에 적고**, 교사가 지정한 곳(구글 클래스룸 댓글, 패들렛 등)에 붙여넣는다
- 3차시 시작 시 같은 코드를 입력하면 1차시 응답이 복원된다 (기기가 바뀌어도 무관)
- 체크섬이 틀리면 "코드를 다시 확인해 주세요" 안내

### 개인정보

- **실명을 저장하지 않는다.** 학번 뒷자리 또는 교사 배부 익명 코드(예: `1-07`)만 사용
- 매칭표는 교사가 종이로 보관

### 서술형 응답

코드에 들어가지 않는다. 별도로 **전체 기록 .txt 다운로드** 버튼을 제공한다.

### 교사 화면 `/teacher`

- 코드 여러 개를 한 번에 붙여넣기 → 즉석 디코딩 → 태그별 비율 막대그래프
- 변인 분류 정확도 (첫 시도 vs 최종)
- 확신도 변화 분포
- 화면 개방 제어: 교사가 "현재 개방 화면"을 지정하면 학생은 그 이후로 못 넘어간다. 먼저 끝낸 학생에게는 대기 화면 + 추가 발문 표시
- 자동 피드백 공개/잠금 토글
- 그래프 수치 표시/숨김 토글

교사 설정은 **localStorage에 저장되고, 학생 기기에는 자동 전파되지 않는다.**
학생 화면에 6자리 "수업 설정 코드"를 입력하는 칸을 두어 교사가 칠판에 적어 주는 방식으로 동기화한다.

---

## 8. 3차시 설계 스튜디오

### 8-1. 코드 복원 / 실측값 입력

1. 1차시 결과 코드 입력 (건너뛰기 가능)
2. 2차시 실물 실험에서 측정한 **완충재 0장·3장의 최대 가속도(g)** 입력 (건너뛰기 가능, 미입력 시 표준값 사용)
3. 즉시 역산 결과 표시:

> 네 측정값으로 계산하면 충돌 시간은 약 **0.013초**, 평균힘은 약 **77 N**이었습니다.
> 완충재가 없을 때와 비교하면 충돌 시간은 **4.3배**, 평균힘은 **약 1/4.3**입니다.
> 충격량은 두 경우 모두 약 **0.98 N·s**로 거의 같습니다.

### 8-2. 상황 선택

야구 포구 / 골키퍼 장갑 / 자전거 헬멧 / 자동차 에어백 / 안전벨트 / 높이뛰기 매트 / 무릎 보호대 / 휴대전화 케이스

각 상황에 기본 질량·속도 프리셋을 넣되 학생이 수정할 수 있게 한다.

### 8-3. 문제 정의

충돌하는 물체 / 보호 대상 / 현재 문제 / 목표 평균힘 / 재료 / 두께·무게 제한

### 8-4. 프롬프트 조립기

학생 입력값을 채워 문장을 자동 완성하고 **[프롬프트 복사]** 버튼을 제공한다.
**AI를 호출하지 않는다.** 학생이 학교에서 쓰는 AI 서비스에 직접 붙여넣는다.

```
질량 [0.2] kg인 [스마트폰]이 [1.2] m 높이에서 떨어져 [4.9] m/s로 충돌한다.
현재 충돌 시간은 약 [0.003]초, 평균힘은 약 [327] N이다.
충돌 시간을 늘려 평균힘을 [100] N 이하로 줄이는 보호 장치를 고안해 줘.
제약 조건: 두께 [15] mm 이하, 무게 [50] g 이하, 재료 [실리콘/에어캡/발포폼].
구조와 과학적 원리를 나누어 설명하고, 이 설계의 한계 세 가지도 함께 알려 줘.
```

마지막 두 줄은 **고정 꼬리표**로 항상 붙는다. 한계를 요구해야 학생이 비평할 거리가 생긴다.

### 8-5. AI 답변 자기 검증 (이 모듈의 핵심)

학생이 AI 답변을 텍스트 영역에 붙여넣으면, **시뮬레이터는 채점하지 않고 되묻는다.**

체크리스트 (학생이 직접 체크 + 근거 서술):
- [ ] AI가 '충격을 흡수한다'고만 썼는가, **충돌 시간**을 언급했는가?
- [ ] AI가 충격량과 충격력을 구분했는가?
- [ ] AI가 제시한 두께·재질로 계산하면 평균힘은 얼마인가? → 숫자 입력 → 엔진 계산 결과 표시
- [ ] 무게·부피·비용 제약을 위반하지 않는가?
- [ ] **AI 답변 중 내가 버린 것과 그 이유는?** (서술 필수)

마지막 항목이 채점기준 A수준("창의적으로 고안하고 과학적 근거를 논리적으로 설명")에 직결된다.

### 8-6. 설계 캔버스와 판정

학생이 조절: 완충 두께 / 재질 강성 / 접촉 면적
엔진이 계산: 충돌 시간 / 평균힘 / 최대힘 / 충격량

**판정은 점수화하지 않는다.** 통과 / 미통과 + 사유 문구로만 표시한다.

```
✅ 평균힘 목표 달성      92 N  (목표 100 N 이하)
❌ 두께 제한 초과        22 mm (제한 15 mm)
   → 두께를 줄이면 충돌 시간이 짧아져 평균힘이 다시 커집니다. 어떻게 해결할까요?
⚠️ 무게 증가             68 g  (제한 50 g)
```

미통과 사유 문구는 답을 주지 않고 되묻는 형식으로 작성한다.

### 8-7. 산출물 출력

문제 정의 / 사용한 프롬프트 / AI 답변 / 자기 검증 결과 / 최종 설계값 / 판정 결과를
**한 장짜리 인쇄용 화면**으로 출력한다. (`window.print()` 사용, PDF 라이브러리 불필요)

---

## 9. 구현 순서

각 단계가 끝날 때마다 브라우저에서 동작을 확인한 뒤 다음으로 넘어간다.

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 1 | 프로젝트 셋업 + 물리 엔진 순수 함수 + 테스트 | 콘솔에 계산값이 명세와 일치 |
| 2 | 상태 저장소(Zustand) + localStorage + 라우팅 + 레이아웃 | 빈 화면 5개가 순서대로 이동 |
| 3 | 화면 1 (수레) + 그래프 컴포넌트 | 손 뗀 후 v–t가 수평선, 마찰 슬라이더 작동 |
| 4 | 화면 0·2 (충돌·면적 불변 데모) | 두 곡선 면적이 같게 표시 |
| 5 | 화면 3 (변인 카드) | 갤럭시탭에서 드래그·탭 모두 작동 |
| 6 | 화면 5 + 출구 문항 + 결과 코드 발급 | 코드 발급 후 재입력 시 복원 |
| 7 | 교사 화면 | 코드 여러 개 붙여넣기 → 집계 표시 |
| 8 | 3차시 설계 스튜디오 | 프롬프트 복사, 판정 표시, 인쇄 출력 |

---

## 10. 완료 판정 체크리스트

- [ ] 예측을 저장하지 않으면 결과 재생 버튼이 눌리지 않는다
- [ ] 최초 응답 저장 후에는 수정할 수 없다
- [ ] 화면2까지 수치가 숨겨져 있다
- [ ] 자동 피드백이 기본적으로 잠겨 있다
- [ ] 화면2에서 두 곡선의 면적이 같게 표시된다
- [ ] 변인 카드가 갤럭시탭 크기(1280×800) 터치로 이동한다
- [ ] 탭-투-탭 조작이 드래그 없이도 작동한다
- [ ] 통제 변인 칸이 비면 다음으로 넘어갈 수 없다
- [ ] 결과 코드가 18자 이내이고, 재입력하면 응답이 복원된다
- [ ] 네트워크가 끊긴 상태에서 처음부터 끝까지 진행된다
- [ ] 어떤 화면에도 fetch/외부 API 호출이 없다
