# AGENTS.md

이 프로젝트에서 작업하는 에이전트는 아래 규칙을 반드시 지킨다.
상세 명세는 `spec.md`에 있다. **작업을 시작하기 전에 항상 `spec.md`를 먼저 읽는다.**

---

## 이 프로젝트가 무엇인가

고등학교 통합과학1 수업에서 학생이 직접 사용하는 웹 시뮬레이터다.
교실에서 45분 동안 학생 25명이 각자의 기기로 동시에 사용한다.
**수업 도중 멈추면 그 시간은 되돌릴 수 없다.** 안정성이 기능보다 우선한다.

---

## 절대 금지 사항

1. **네트워크 요청 금지.** `fetch`, `axios`, `XMLHttpRequest`, 외부 CDN 링크를 사용하지 않는다.
   학교망에서 차단되면 수업이 중단된다. 모든 것은 오프라인에서 작동해야 한다.

2. **백엔드·데이터베이스 금지.** Firebase, Supabase, Express 서버를 만들지 않는다.
   저장은 `localStorage`뿐이다.

3. **AI API 호출 금지.** 3차시 AI 활동은 "프롬프트 문자열 생성 + 클립보드 복사"까지만 구현한다.
   학생이 외부 AI 서비스에 직접 붙여넣는다.

4. **HTML5 Drag and Drop API 금지.** `draggable`, `onDragStart`, `onDrop`을 쓰지 않는다.
   갤럭시탭에서 작동하지 않는다. Pointer Events로 직접 구현하고, 탭-투-탭 대체 조작을 함께 넣는다.

5. **물리 상수를 임의로 바꾸지 않는다.** 특히 다음 두 값은 교육적 의도가 있다.
   - 정지 거리 지수 `0.85` (완충재 수와 충격력이 정확한 반비례가 아니게 만든다)
   - 피크/평균 계수 `1.57` (평균힘과 최대힘이 다르다는 것을 보여준다)
   "더 깔끔한 값"으로 정리하지 말 것.

6. **정답을 먼저 보여주지 않는다.** 학생이 예측을 저장하기 전에 결과·정답·해설을 노출하는 UI를 만들지 않는다.

7. **오개념 태그 코드(IMP-1 등)를 학생 화면에 표시하지 않는다.** 교사 화면 전용이다.

---

## 코드 작성 규칙

- **물리 계산은 `src/engine/` 안의 순수 함수로만.** React import 금지, 부수 효과 금지.
- **애니메이션과 그래프는 같은 계산 결과를 공유한다.** 엔진이 시계열 배열을 한 번 만들고, 애니메이션과 그래프가 그것을 함께 본다. 각자 따로 계산하면 값이 어긋난다. 이 실수가 가장 흔하다.
- TypeScript `any` 금지. 모든 상태에 타입을 정의한다.
- 한 파일 300줄을 넘기지 않는다. 넘으면 분리한다.
- 화면에 표시되는 모든 문구는 **한국어**다. 코드 주석도 한국어로 쓴다.
- 변수·함수명은 영어로 쓰되, 명세서 용어를 따른다 (`impulse`, `avgForce`, `peakForce`, `collisionTime`).

---

## UI 규칙

- 대상 기기: 노트북(1366×768 이상)과 갤럭시탭 가로(1280×800). 두 곳에서 모두 확인한다.
- 터치 대상 최소 44×44 px, 본문 글자 16px 이상.
- 색상만으로 정보를 구분하지 않는다. 색 + 아이콘 + 글자를 함께 쓴다.
- 학생용 화면에 개발자용 정보(콘솔 로그, 디버그 패널, 에러 스택)를 노출하지 않는다.
- 로딩 상태와 빈 상태를 항상 처리한다. 화면이 하얗게 비는 순간이 없어야 한다.

---

## 작업 방식

- **한 번에 한 단계씩** 진행한다. `spec.md` §9의 구현 순서를 따른다.
- 각 단계를 마치면 **개발 서버를 띄우고 브라우저로 직접 확인**한 뒤, 스크린샷과 함께 보고한다.
- 명세에 없는 기능을 추가하지 않는다. 필요해 보이면 먼저 물어본다.
- 명세가 모호하면 추측하지 말고 질문한다. 이 프로젝트는 교육적 의도가 세부 사항에 들어 있어서, 그럴듯한 추측이 자주 틀린다.
- 기존에 작동하던 화면을 건드리게 되면 먼저 알린다.

---

## 완료 보고 형식

각 단계를 마치면 다음을 보고한다.

1. 무엇을 만들었는가 (파일 목록)
2. 브라우저에서 확인한 결과 (스크린샷)
3. `spec.md` §10 체크리스트 중 이번 단계에 해당하는 항목의 통과 여부
4. 다음 단계에서 확인이 필요한 사항
